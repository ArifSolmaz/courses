# Archived AA material (September 2026)

Not published and not part of the build. Kept for provenance:

- `legacy-guide-sequence/` — Markdown for the former 14-week Python-first sequence (superseded by `tools/aligned/course.py`).
- `visual_lessons.py` and the verifiers of the former routes (`verify_guide.py`, `verify_question_bank.py`, `verify_visual_lessons.py`, `verify_skiena_material.py`). They asserted the old sequence and already failed before the redesign.
- `*_REVIEW.md` — internal review notes from earlier iterations.

`tools/build_legacy.py` and `tools/build_guide.py` stay in `tools/` because the current build still uses their page chrome and the Week 1 stories.
